{
  "name": "lets-be-canadian-back",
  "version": "0.0.1",
  "description": "Application backend",
  "author": "William DENORME",
  "license": "ISC",
  "main": "src/index.ts",
  "scripts": {
    "start": "node dist/index.js",
    "build": "npx tsc",
    "clean": "rm -rf dist && rm -rf node_modules",
    "setup": "npm ci --omit=dev && npm run build"
  },
  "dependencies": {
    "fastify": "^4.26.2",
    "fastify-mongodb": "^4.2.0",
    "fastify-plugin": "^4.5.1",
    "mongoose": "^8.3.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.1.1",
    "eslint": "^9.1.1",
    "typescript": "^5.4.5"
  }
}
